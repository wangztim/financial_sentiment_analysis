name: stocktwits_nlp
channels:
  - defaults
dependencies:
  - requests
  - autopep8
  - pandas
  - jupyterlab
  - python=3.8
  - pytorch
  - seaborn
  - aiohttp
  - pip:
      - tokenizers==0.9.2
      - transformers==3.4.0
prefix: /usr/local/Caskroom/miniconda/base/envs/stocktwits_nlp
