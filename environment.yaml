name: stocktwits_nlp
channels:
  - defaults
dependencies:
  - requests
  - autopep8
  - pandas
  - jupyterlab
  - python=3.8
  - pytorch
  - seaborn
  - aiohttp
  - pip
  - pyarrow
prefix: /usr/local/Caskroom/miniconda/base/envs/stocktwits_nlp
